// let variable = function () {};

// // __proto__ es un objeto que contiene las propiedades y métodos que son comunes
// // si queremos accedere a las propiedades de un objeo que creamos nosotros, se tiene que usar prototype
// console.log(variable.prototype.__proto__);

let variable = ["pedro", 34, null];

console.log(variable);
